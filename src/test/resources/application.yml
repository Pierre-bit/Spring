server:
  port: 8380
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:calendrier_gif_test
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    show-sql: true
